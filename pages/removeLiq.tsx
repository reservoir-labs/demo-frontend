import { Default } from 'components/layouts/Default';
import { RemoveLiq } from "components/templates/removeLiq";

const RemoveLiqPage = () => {
    return (
        <Default pageName="Remove Liquidity" >
            <RemoveLiq />
        </Default>
    );
};

export default RemoveLiqPage;
